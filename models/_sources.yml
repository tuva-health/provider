version: 2

sources:
  - name: nppes
    description: Raw provider data from NPPES, NUCC, and CMS
    database: nppes
    schema: raw_data
    tables:
      - name: npi
      - name: npi_othername
      - name: nucc_taxonomy
      - name: medicare_specialty_crosswalk
